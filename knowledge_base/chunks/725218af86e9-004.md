# skill-issue-form/server.py

类型：测试验收
关键词：OTA, 测试, 验收

---

option>算法问题</option>
 <option>数据问题</option>
 <option>配置问题</option>
 <option>需求不清</option>
 </select>
 </label>
 <label class="only-a">修改文件路径
 <input name="file_path" placeholder="skills/hotel-ota/s05-.../SKILL.md" />
 </label>
 <label class="only-a">是否已在服务器测试
 <select name="server_tested">
 <option>否</option>
 <option>是</option>
 </select>
 </label>
 <label class="only-b">涉及功能
 <input name="feature" placeholder="例如：S6 execute-price 权限闸门" />
 </label>
 <label class="only-b">是否影响生产安全
 <select name="production_safety">
 <option>否</option>
 <option>是</option>
 </select>
 </label>
 <label class="full only-a">当前问题
 <textarea name="current_problem" placeholder="请说明为什么要改。"></textarea>
 </label>
 <label class="full only-a">用户话术或测试场景
 <textarea name="scenario" placeholder="触发这个修改的场景。"></textarea>
 </label>
 <label class="full only-a">原输出哪里不符合预期
 <textarea name="bad_output"></textarea>
 </label>
 <label class="full only-a">期望达到什么效果
 <textarea name="target_effect"></textarea>
 </label>
 <label class="full only-a">修改前
 <textarea name="before_change" placeholder="粘贴原来的关键内容，不需要贴整个文件。"></textarea>
 </label>
 <label class="full only-a">修改后
 <textarea name="after_change" placeholder="粘贴修改后的关键内容。"></textarea>
 </label>
 <label class="full">测试输入/命令
 <textarea name="test_input" placeholder="@机器人 今天璞悦需要调价吗？或 CLI 命令"></textarea>
 </label>
 <label class="full">实际输出/当前输出
 <textarea name="actual_output" placeholder="粘贴实际回复或关键 JSON 字段"></textarea>
 </label>
 <label class="full only-b">错在哪里
 <textarea name="wrong_reason"></textarea>
 </label>
 <label class="only-b">是否稳定复现
 <select name="reproducible">
 <option>是</option>
 <option>否</option>
 <option>不确定</option>
 </select>
 </label>
 <label class="full">期望输出
 <textarea name="expected_output" placeholder="希望它应该怎么返回"></textarea>
 </label>
 <label class="full only-b">正确逻辑应该是
 <textarea name="expected_logic"></textarea>
 </label>
 <label class="full only-b">哪些数据可以用
 <textarea name="usable_data"></textarea>
 </label>
 <label class="full only-b">哪些数据必须阻断
 <textarea name="blocked_data"></textarea>
 </label>
 <label class="full only-b">建议修改位置
 <textarea name="suggested_location" placeholder="runtime 文件、函数或命令、数据表/字段等。"></textarea>
 </label>
 <label class="full only-b">建议修改内容
 <textarea name="suggested_change"></textarea>
 </label>
 <label class="full only-b">验收标准
 <textarea name="acceptance" placeholder="单测、CLI、飞书话术、不能出现的错误输出。"></textarea>
 </label>
 <label class="full only-c">为什么这样判断
 <textarea name="judgement_reason"></textarea>
 </label>
 <label class="full only-c">复现步骤
 <textarea name="repro_steps" placeholder="1.&#10;2.&#10;3."></textarea>
 </label>
 <label class="full only-c">是否影响其他 skill
 <textarea name="impact" placeholder="影响 / 不影响 / 不确定，并说明原因。"></textarea>
 </label>
 <label class="full only-a">是否符合预期
 <select name="pass_status">
 <option>通过</option>
 <option>未通过</option>
 <option>部分通过</option>
 </select>
 </label>
 <label class="full only-a">风险与注意事项
 <textarea name="risk_notes" placeholder="是否影响其他 skill、是否涉及审批/真实数据/飞书外发安全、需要重点检查的地方。"></textarea>
 </label>
 <label class="full upload">截图/图片
 <input name="screenshots" type="file" accept="image/*" multiple />
 <div class="paste-zone" id="paste-zone" tabindex="0">点击这里后按 Cmd+V 粘贴截图，也可以直接拖入/选择图片</div>
 <div class="paste-preview" id="paste-preview"></div>
 <span class="hint">支持选择文件、拖入图片、或截图后直接 Cmd+V；提交后会保存并插入 Word 文档。</span>
 </label>
 </div>
 <div class="actions">
 <button type="submit">提交并写入 Word</button>
 <span id="status"></span>
 </div>
 </form>
 </section>
 <section>
 <div class="toolbar">
 <h2>已提交问题</h2>
 <span class="count" id="count">0 条</span>
 </div>
 <div id="issues" class="empty">还没有提交记录</div>
 </section>
 </main>
 <script>
 const form = document.getElementById('issue-form');
 const templateType = document.getElementById('template-type');
 const statusEl = document.getElementById('status');
 const issuesEl = document.getElementById('issues');
 const countEl = document.getElementById('count');
 const fileInput = form.querySelector('input[name="screenshots"]');
 const pasteZone = document.getElementById('paste-zone');
 const pastePreview = document.getElementById('paste-preview');
 let pastedImages = [];

 function esc(value) {{
 return String(value || '').replace(/[&<>"']/g, char => ({{
 '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;'
 }}[char]));
 }}

 function render(records) {{
 countEl.textContent = records.length + ' 条';
 if (!records.length) {{
 issuesEl.className = 'empty';
 issuesEl.textContent = '还没有提交记录';
 return;
 }}
 issuesEl.className = '';
 issuesEl.innerHTML = records.slice().reverse().map(item => `
 <article class="issue">
 <div class="issue-head">
 <h3>${{esc(item.id)}}｜${{esc(item.template_name || item.template_type || '模板 C')}}｜${{esc(item.skill_id || '未填写 Skill')}}</h3>
 <button class="delete-issue" type="button" data-id="${{esc(item.id)}}">删除</button>
 </div>
 <div class="meta">
 <span class="pill">${{esc(item.skill_id)}}</span>
 <span class="pill">严重程度：${{esc(item.severity)}}</span>
 <spa
