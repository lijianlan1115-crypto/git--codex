# hotel--ota-ai 2/skills/hotel-ota/s14-operation-diagnosis/references/runtime_commands.md

类型：项目资料
关键词：OTA, OpenClaw, 诊断

---

# S14 酒店运营诊断 runtime 命令

OpenClaw 调用时继续使用兼容入口 `python runtime/hotel_ota_runtime.py ...`。

## 可用命令
- `python runtime/hotel_ota_runtime.py ota-health --hotel-id puyue`
- `python runtime/hotel_ota_runtime.py conversion-diagnosis --hotel-id puyue`
- `python runtime/hotel_ota_runtime.py database-query --db-kind mysql --template operating_snapshot --hotel-id puyue`
- `python runtime/hotel_ota_runtime.py database-query --db-kind mysql --template daily_metrics --hotel-id puyue`

## 规则
- 不直接调用 runtime 内部模块路径。
- 真实写动作必须加审批和 dry-run 预览。
- API 未确认时优先使用 `normalize-sample`、manual upload 或 RPA 输入。
- MySQL 日经营指标使用 `metric_aliases` 归一化，未确认指标名时标记 `manual_required`。
