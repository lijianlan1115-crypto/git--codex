# hotel--ota-ai 2/skills/hotel-ota/s16-progress-deviation/references/runtime_commands.md

类型：字段契约
关键词：OTA, 订单, 字段, OpenClaw, 诊断

---

# S16 进度偏差诊断 runtime 命令

OpenClaw 调用时继续使用兼容入口 `python runtime/hotel_ota_runtime.py ...`。

## 可用命令
- `python runtime/hotel_ota_runtime.py deviation --hotel-id puyue`
- `python runtime/hotel_ota_runtime.py database-query --db-kind mysql --template operating_snapshot --hotel-id puyue`
- `python runtime/hotel_ota_runtime.py database-query --db-kind mysql --template order_snapshot --hotel-id puyue`
- `python runtime/hotel_ota_runtime.py database-query --db-kind mysql --template operation_diagnosis --hotel-id puyue`
- `python runtime/hotel_ota_runtime.py database-query --db-kind mysql --template daily_metrics --hotel-id puyue`
- `python runtime/hotel_ota_runtime.py database-query --db-kind mysql --template monthly_metrics --hotel-id puyue`

## 规则
- 不直接调用 runtime 内部模块路径。
- 真实写动作必须加审批和 dry-run 预览。
- API 未确认时优先使用 `normalize-sample`、manual upload 或 RPA 输入。
- `deviation` 当前主输出字段为 `direction`、`actual_orders`、`target_orders`、`progress_checkpoint`、`checkpoint_target_orders`、`checkpoint_completion_rate`、`field_freshness`、`downstream_allowed`。
- `order_snapshot` 是实际订单优先来源；缺失时 runtime 可参考 `operating_snapshot` 的今日已售字段，但代理字段不得用于 live 调价。
- `daily_metrics.room_nights` 不能冒充实时实际进度，只能作为目标或对比证据。
- `downstream_allowed=false` 时，skill 只能输出 S14/S9/S3 等诊断或通知建议，不得触发正式 S5/S6 调价链路。
- MySQL 指标只作为偏差证据来源；指标别名变化时改 `database-source.json`。
