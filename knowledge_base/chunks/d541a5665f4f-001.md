# hotel--ota-ai 2/skills/hotel-ota/s11-promotion-execution/SKILL.md

类型：字段契约
关键词：字段, 飞书

---

﻿---
name: s11-promotion-execution
description: "S11 推广执行：审批后执行或下发推广开关、出价、预算和活动任务。触发语：执行推广、调整出价、关闭广告、打开广告。"
---

# S11 推广执行

## Reference 导航

执行本 skill 前先读取本目录 `references/` 五件套：

- `references/input_schema.json`：输入字段和字段质量。
- `references/output_schema.json`：统一输出结构。
- `references/rules.md`：多源依据、判断逻辑、异常和安全规则。
- `references/examples.md`：飞书话术、样例输入、runtime 输出和最终回复。
- `references/runtime_commands.md`：可调用的稳定脚本命令。

## 适用场景

仅当用户明确要求执行已审批推广动作，或需要生成前台/平台人工任务时，使用本 skill。

## 必须读取

- `{baseDir}/../_shared/common-contract.md`
- `{baseDir}/../_shared/operating-policy.md`

## 安全规则

- 默认只生成 dry-run 或任务，不直接真实修改。
- 预算、出价、活动变更必须 admin/owner 审批。
- 美团活动/推广 API 未确认时，不做真实 API 执行，只生成 RPA 或人工任务和截图要求。

## 执行流程

1. 校验审批和动作明细。
2. 将 S8/S10 的建议转为执行任务。
3. 明确平台、动作、金额、时间窗口、回滚条件、复核人。
4. 通过 S3 消息中台发送。
5. 记录执行结果和下一次复核时间。

## 输出要求

返回执行状态、任务卡片、审批 ID、复核清单。
