# hotel--ota-ai/skills/hotel-ota/s01-control-config/references/runtime_commands.md

类型：项目资料
关键词：OTA, OpenClaw

---

# S1 总控配置 runtime 命令

OpenClaw 调用时继续使用兼容入口 `python runtime/hotel_ota_runtime.py ...`。

## 可用命令
- `python runtime/hotel_ota_runtime.py init-db`
- `python runtime/hotel_ota_runtime.py seed-demo`

## 规则
- 不直接调用 runtime 内部模块路径。
- 真实写动作必须加审批和 dry-run 预览。
- API 未确认时优先使用 `normalize-sample`、manual upload 或 RPA 输入。
